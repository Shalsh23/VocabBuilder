services:
  - type: web
    name: vocabbuilder
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "cd web && gunicorn app:app --bind 0.0.0.0:$PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
